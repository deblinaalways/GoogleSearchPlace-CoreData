# Uncomment the next line to define a global platform for your project
platform :ios, '9.0'
use_frameworks!

def searchplace_pods
pod 'GooglePlaces'
end

target ‘SearchPlace’ do
    searchplace_pods
end
